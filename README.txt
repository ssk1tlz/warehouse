═══════════════════════════════════════════════════════════════
  СКЛАД IT-ТЕХНИКИ - Инструкция по использованию
═══════════════════════════════════════════════════════════════

📦 ЗАПУСК ПРИЛОЖЕНИЯ
────────────────────────────────────────────────────────────────
Способ 1 (основной) — приложение в трее:
  ► Дважды кликните на WarehouseApp.exe

  • В системном трее появится иконка приложения
  • Сервер запустится автоматически, откроется браузер
  • Повторный запуск НЕ создаст второй экземпляр —
    просто откроется новая вкладка браузера

Способ 2 (альтернативный, нужен Python):
  ► Дважды кликните на start_server.bat

  • Скрипт проверит, не запущен ли уже сервер
  • Запустит сервер в свернутом окне и откроет браузер


🛑 ОСТАНОВКА ПРИЛОЖЕНИЯ
────────────────────────────────────────────────────────────────
Если запускали WarehouseApp.exe:
  ► Правый клик по иконке в трее → «Выход»
    (при выходе автоматически создается резервная копия базы)

Если запускали start_server.bat:
  ► Дважды кликните на stop_server.bat
    или закройте окно консоли «Warehouse Server»


🔄 МЕНЮ В ТРЕЕ (правый клик по иконке)
────────────────────────────────────────────────────────────────
  • Открыть приложение  — открыть вкладку в браузере
  • Статус              — состояние сервера
  • Перезапустить сервер — перезапуск без выхода из приложения
  • Выход               — остановка сервера + резервная копия


📂 РЕЗЕРВНЫЕ КОПИИ
────────────────────────────────────────────────────────────────
Автоматические резервные копии базы данных сохраняются в:
  📁 backups\

  • Копия создается перед каждым сохранением данных
    и при выходе из приложения
  • Хранятся последние 30 копий
  • Старые версии программы лежат в backups\old_exe\

Чтобы восстановить базу: остановите приложение, замените
warehouse.db на нужный файл из backups\ (переименуйте его
в warehouse.db) и запустите приложение снова.


🌐 ДОСТУП К ПРИЛОЖЕНИЮ
────────────────────────────────────────────────────────────────
После запуска приложение доступно по адресу:
  http://127.0.0.1:8765/

⚠️  Работайте с приложением в ОДНОЙ вкладке браузера.
    Если открыть несколько вкладок, при одновременном
    редактировании сервер примет только первое изменение,
    а второй вкладке предложит обновить данные.


🖥  УСТАНОВКА НА СЕРВЕР (доступ по локальной сети)
────────────────────────────────────────────────────────────────
Приложение можно поставить на сервер, чтобы работать с ним
с любого компьютера в локальной сети:

1. Скопируйте всю папку приложения на сервер
2. На сервере запустите setup_lan.bat
   ► от имени администратора (правый клик → Запуск от имени
     администратора)
   Скрипт создаст config.json и откроет порт 8765 в брандмауэре,
   а в конце покажет адрес для подключения
3. Запустите WarehouseApp.exe (или start_server.bat)
4. (по желанию) Запустите add_autostart.bat — приложение будет
   стартовать автоматически при входе в систему
5. На остальных компьютерах просто откройте в браузере:
   http://<IP-адрес-сервера>:8765/
   (адрес показывает setup_lan.bat; можно сделать закладку)

Вернуть локальный режим: setup_local.bat (от имени администратора).

Несколько человек могут работать одновременно: если двое
сохранят изменения сразу, сервер примет первое, а второму
предложит обновить данные и повторить действие.

⚠️  ВНИМАНИЕ: входа по паролю нет — данные сможет менять любой,
    у кого есть доступ к вашей локальной сети. Не открывайте
    порт 8765 в интернет.


⚙️  НАСТРОЙКИ (config.json)
────────────────────────────────────────────────────────────────
Файл config.json рядом с приложением (необязательный):
  {
    "host": "0.0.0.0",   ← "127.0.0.1" = только этот компьютер,
                            "0.0.0.0" = доступ по сети
    "port": 8765         ← порт сервера
  }
Если файла нет — приложение работает только локально.
После изменения настроек перезапустите приложение.


🔧 ПЕРЕСБОРКА EXE
────────────────────────────────────────────────────────────────
После изменения кода (server.py, app.js, index.html и т.д.)
пересоберите exe-файл:
  ► Дважды кликните на rebuild_and_replace.bat
Подробности — в файле КАК_ПЕРЕСОБРАТЬ.txt


⚙️  ТРЕБОВАНИЯ
────────────────────────────────────────────────────────────────
  • WarehouseApp.exe — ничего не требует (всё включено)
  • start_server.bat — Python 3.12+
  • Браузер (Chrome, Firefox, Edge и т.д.)


❓ ПРОБЛЕМЫ
────────────────────────────────────────────────────────────────
Если приложение не запускается:

1. Запустите stop_server.bat
2. Подождите 5 секунд
3. Запустите WarehouseApp.exe снова

Если проблема сохраняется, проверьте:
  • Не занят ли порт 8765 другим приложением
  • Есть ли права на запись в папку d:\warehouse\
  • Не остался ли файл .warehouse_app.lock (можно удалить,
    если приложение точно не запущено)


═══════════════════════════════════════════════════════════════
  © 2026 Склад IT-техники
═══════════════════════════════════════════════════════════════
