# .bat разбирается cmd.exe, который требует CRLF в многострочных блоках if(...).
# Без этого правила файл на клоне с core.autocrlf=input придёт с LF и сборка
# упадёт с невнятной синтаксической ошибкой.
*.bat text eol=crlf
